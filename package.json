{
  "name": "primary",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cham-browser build --config config.yaml",
    "dev": "cham-browser dev --config config.yaml",
    "preview": "npx serve dist"
  },
  "dependencies": {
    "@hanology/cham": "^0.1.10",
    "@hanology/cham-browser": "^0.4.76",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "puppeteer": "^25.0.4"
  }
}
